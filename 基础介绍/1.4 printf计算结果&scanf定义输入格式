int main()
{
    // 定义变量
    int a;
    int b;

    printf("请输入两个整数：");
    
    //这里scanf里定义输入的两个值以空格隔开，如果输入，会导致运行结果错误
    scanf("%d %d", &a, &b);
    printf("%d + %d = %d", a, b, a+b);

    return 0;
}
