
指针&: 取得变量的地址 
获取变量地址： int i; printf("%p", &i);
地址大小与int是否相同取决于编译器
必须对变量取地址

int *p; //定义指针变量
p = &i; //指针变量存放变量地址
数组变量是特殊的指针（const指针）（函数参数表里int a[]和int *a等价）（*p = p[0]）
const int *p(const在*前)：指针所的东西不能修改
int *const p（const在*后）：指针本身不能修改

指针+1：加一个sizeof(指针所指向的变量的数据类型)
两个指针相减：之差除以sizeof
不同类型的指针不能互相运算